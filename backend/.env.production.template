# ==================================================
# IDURAR ERP/CRM Backend - Production Environment
# ==================================================
# Copy this file to .env and fill in your actual values

# ==================================================
# DATABASE CONFIGURATION (REQUIRED)
# ==================================================
# MongoDB Connection String
# Format: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
DATABASE="mongodb+srv://sripadv2000_db_user:l3zH5FROgfGPDnKD@cluster0.5qhzpwc.mongodb.net/idurar-erp-crm?retryWrites=true&w=majority&appName=Cluster0"

# ==================================================
# SECURITY CONFIGURATION (REQUIRED)
# ==================================================
# JWT Secret - CHANGE THIS to a strong random string (minimum 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MINIMUM_32_CHARS"

# ==================================================
# SERVER CONFIGURATION
# ==================================================
# Node Environment
NODE_ENV="production"

# Port (Cloud Run will automatically set this, but 8080 is the default)
PORT=8080

# Public Server URL - Set this to your deployed backend URL
# Example: https://your-backend-service-abc123.run.app/
PUBLIC_SERVER_FILE="https://your-backend-service.run.app/"

# ==================================================
# CORS CONFIGURATION
# ==================================================
# CORS Allowed Origins - Comma-separated list of frontend URLs
# Use "*" for development only (security risk in production)
# Example: https://your-frontend.run.app,https://your-custom-domain.com
CORS_ORIGIN="https://your-frontend-service.run.app"

# ==================================================
# OPTIONAL CONFIGURATIONS
# ==================================================
# OpenSSL Configuration (required for PDF generation)
OPENSSL_CONF="/dev/null"

# Email Service API Key (for password reset emails)
# Get API key from: https://resend.com/
# RESEND_API=""

# OpenAI API Key (for AI features)
# Get API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=""

# AWS S3 Configuration (if using file uploads to S3)
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
# AWS_REGION="us-east-1"
# AWS_S3_BUCKET_NAME=""

# ==================================================
# CLOUD RUN DEPLOYMENT NOTES
# ==================================================
# 1. Never commit this file with real credentials to git
# 2. Set these as environment variables in Cloud Run console or via gcloud CLI
# 3. Required variables: DATABASE, JWT_SECRET, PUBLIC_SERVER_FILE, CORS_ORIGIN
# 4. Cloud Run will automatically set PORT (do not override)
# ==================================================
