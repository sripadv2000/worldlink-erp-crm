# Frontend Environment Configuration for Cloud Run

# Backend Server URL (REQUIRED for production)
# Set this to your Cloud Run backend service URL
# Example: https://your-backend-service.run.app/
VITE_BACKEND_SERVER=http://backend:8080/

# File Base URL (typically same as backend server)
VITE_FILE_BASE_URL=http://backend:8080/

# Frontend URL (optional, will use window.location.origin if not set)
# VITE_FRONTEND_URL="https://your-frontend-service.run.app/"

# Production flag (set automatically by Vite during build)
# PROD=false